
export interface Receta {
    nombre: string;
    descripcion: string;
    foto: string; // Puedes usar una URL de imagen o almacenar la imagen de alguna otra manera
    proceso: string;
    video: string; // URL del video de preparación (opcional)
  }
  